import { n as RemoteModuleRenderOptions, t as Config } from "./_chunks-dts/config.js";
import { a as disconnectApp, i as defineStateTopics, r as connect, t as ConnectOptions } from "./_chunks-dts/bus.js";
/**
 * Options for rendering the workbench.
 *
 * @public
 */
interface RenderWorkbenchOptions extends RemoteModuleRenderOptions {}
declare global {
  interface ImportMetaEnv {
    readonly SANITY_INTERNAL_WORKBENCH_REMOTE_URL: string;
  }
  interface ImportMeta {
    readonly env: ImportMetaEnv;
  }
}
/**
 * Creates a Module Federation instance, loads a remote workbench
 * application, and renders it into the provided root element.
 *
 * @param rootElement - The DOM element to render into
 * @param config - Workbench configuration, forwarded to the remote. The CLI
 *   sources `organizationId` from `unstable_defineApp` (required there).
 * @param options - Rendering options forwarded to the remote
 * @returns A cleanup function that unmounts the remote application
 *
 * @public
 */
declare function renderWorkbench(rootElement: HTMLElement, config: Config, options?: RenderWorkbenchOptions): Promise<() => void>;
export { type Config, type ConnectOptions, type RenderWorkbenchOptions, connect, defineStateTopics, disconnectApp, renderWorkbench };